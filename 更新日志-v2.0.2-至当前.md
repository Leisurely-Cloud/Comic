# 漫画下载器更新日志：v2.1.0

对比基准：Git 标签 `v2.0.2`（`08a0ca4`）到当前 `v2.1.0` 代码。

## 新增与增强

- 漫画柜首页发现已接入排行榜入口，支持日排行、周排行、月排行、总排行；原有站内搜索和手动 URL 下载继续保留。
- 下载结束状态更细分，能够区分下载完成、部分完成、用户停止、失败、无可下载章节等状态，避免部分失败被误判为完全成功。
- 下载失败时会记录失败章节信息，并在日志和漫画元数据中保存失败章节号、失败原因和最终下载状态，便于后续排查或重试。
- 下载完成后的整本 ZIP 打包增加进度反馈，打包过程中会显示已处理条目数和写入文件数，成功或失败时同步更新进度条样式。
- 包子漫画图片下载改为流式下载并增大分块大小，下载结束后显式关闭响应对象，降低大图或长章节下载时的内存和连接占用。
- 已完整存在的章节现在会按图片总数返回成功数量，不再返回 `0`，下载统计更准确。
- 章节图片未全部下载成功时会抛出“不完整”错误，避免静默留下缺页章节。
- 无命令行参数启动 `downcomic.py` 时会自动进入图形界面，减少双入口使用时的困惑。
- Windows 控制台启动时会尝试切换 stdout/stderr 到 UTF-8，避免中文或 emoji 日志在 GBK 控制台中触发编码错误。

## 代理与连接

- 代理池底层能力增强，新增宽松/严格两种验证模式。
- 代理验证支持多个 HTTP/HTTPS 测试目标，宽松模式任一目标通过即可保留节点，严格模式要求 HTTPS 测试目标全部通过。
- 已验证代理会持久化到本地缓存，短时间内重启程序可复用已验证节点，减少重复抓取和验证等待。
- 代理抓取和验证流程支持停止信号，GUI 停止任务或连通性测试时更容易中断后台验证。
- 站点连接错误判断和排查文案抽到公共模块，后续扩展站点时可复用同一套提示逻辑。

## 界面与交互

- 首页发现和详情区的 Pane 布局管理抽到 `PaneLayout`，改善窗口缩放、最小化恢复后的分隔比例恢复。
- UI 样式配置抽到 `ui_styles.py`，主 GUI 文件更聚焦业务流程。
- 日志刷新改为批量插入并按区间补标签，降低大量下载日志刷新时的 UI 抖动。
- 排行榜列表刷新时会临时隐藏 Treeview 再回显，减少刷新过程中的残留和闪烁。
- 本地漫画库继续支持本地状态、更新检查、打开目录和 CBZ 导出，并兼容默认下载目录与旧项目目录中的历史下载内容。

## 站点适配与架构

- 原本巨大的 `site_adapters.py` 拆成注册门面和独立站点模块：
  - `base.py`：站点基类、详情数据结构和通用工具。
  - `baozimh.py`：包子漫画适配器。
  - `mangacopy.py`：拷贝漫画适配器。
  - `manhuagui.py`：漫画柜适配器和解析逻辑。
- `site_adapters.py` 继续保留原 import 门面，外部调用方式不变，降低拆分带来的兼容风险。
- 断点续传、失败章节记录等数据处理抽到 `resume_state.py`，减少 GUI 主文件中的状态处理重复。
- 当前工作区还包含若干进一步拆分用的模块草稿，例如 `download_controller.py`、`proxy_panel.py`、`detail_card_panel.py`、`local_library.py`、`archive.py` 等；其中部分能力尚未完全接入主 GUI，本更新日志未把它们作为已发布功能宣传。

## 发布与文档

- README 支持矩阵更新，漫画柜从“首页发现暂不支持”调整为“支持排行榜”。
- README 增补默认下载目录、本地漫画库兼容扫描、CLI 代理池和发布目录说明。
- `create_release.ps1` 不再生成发布 ZIP，只生成本地发布目录、EXE 和使用说明文件，方便按需上传 GitHub Releases。
- 依赖说明补充 `urllib3`。
- 远端 `origin/main` 另有一个 `.gitignore` 清理提交，主要删除误写入忽略列表的具体漫画目录名，属于仓库维护项。

## 修复与稳定性

- 调整 requests 连接池大小和重试策略，减少外层重试与 HTTPAdapter 重试叠加导致的请求次数膨胀。
- 下载任务停止后会更完整地关闭线程池和保存最终状态。
- 失败、停止、部分完成时也会保存元数据，不再只在完全完成时更新本地记录。
- HTML 属性读取增加类型归一化，降低 BeautifulSoup 返回 list/tuple 属性时的解析异常风险。
- 发布脚本文案与实际产物保持一致，不再提示已生成 ZIP。

## 注意事项

- 当前仓库工作区包含未提交改动和新增文件；如果要作为正式版本发布，建议先清理未接入模块或完成接入后再打 tag。
- 当前 `main` 本地分支比本地记录的 `origin/main` 少 1 个 `.gitignore` 提交；如果要对齐 GitHub 最新主分支，发布前应先同步远端。
- 本次只做代码差异整理。尝试运行 `python -m py_compile` 时被当前 `__pycache__` 写入权限阻止，未完成语法编译验证。

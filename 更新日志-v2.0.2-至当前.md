# 漫画下载器更新日志：v2.1.0

对比基准：Git 标签 `v2.0.2`（`08a0ca4`）到当前 `v2.1.0` 代码。

## 这次更新

- 漫画柜首页发现新增排行榜入口，支持日榜、周榜、月榜和总榜，原有搜索和手动链接下载继续可用。
- 下载结果显示更清楚了，现在可以区分下载完成、部分完成、用户停止、失败和无可下载章节等状态。
- 下载失败时会记录失败章节和原因，后续排查或重试更方便。
- 整部漫画打包 ZIP 时增加了进度显示，处理过程更直观。
- `downcomic.py` 在没有命令行参数时会自动进入图形界面，入口更统一。

## 代理与连接

- 代理池增加了宽松和严格两种验证模式，代理可用性判断更灵活。
- 已验证的代理会缓存到本地，短时间内重启程序时可以直接复用。
- 代理抓取和验证过程现在更容易中断，停止任务或测试连接时响应更及时。

## 界面与体验

- 窗口缩放、最小化再恢复后的界面布局更稳定。
- 日志刷新和排行榜列表刷新更顺畅，减少闪烁和抖动。
- 本地漫画库继续支持状态记录、更新检查、打开目录和 CBZ 导出，也兼容旧目录里的历史下载内容。

## 稳定性改进

- 下载统计更准确，已完整存在的章节不会再被错误记为 `0`。
- 章节图片没下完整时会明确报错，避免静默留下缺页内容。
- 包子漫画图片下载的内存和连接占用进一步优化，长章节和大图场景更稳一些。
- 下载任务停止后，状态保存和收尾处理更完整。

## 文档与发布

- README 已同步更新，补充了站点支持范围、默认下载目录和发布目录说明。
- `create_release.ps1` 现在只生成本地发布目录、EXE 和使用说明文件，方便手动上传到 GitHub Releases。

## 验证

- 已对全部 Python 文件执行语法编译检查。
- 已使用 PyInstaller 重新打包 Windows EXE，并确认 EXE 版本信息为 `2.1.0`。

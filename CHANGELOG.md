# 更新日志

## 未发布

### 新功能

- 本地书库新增“导入 JM 目录”：可先扫描外部数字目录并预览漫画、章节、图片与占用空间，只复制缺失章节，自动跳过已存在章节；图片数量不一致的冲突章节保留本地版本且不覆盖。

### 稳定性

- JM 导入采用临时目录和原子移动提交，单部漫画复制失败或取消时会回滚本轮新增内容，且始终保留原始目录。

## v2.5.0 — 2026-08-25

### 新功能

- 新增“每周必看”页面，直接读取 JM 官方推荐期数和作品类型，支持韩漫、其他、日漫切换以及官方内容分类筛选。
- 排行榜与每周必看统一增加内容分类筛选和分类标签，支持同人、单本、短篇、韩漫、其他类等官方分类；标签自动适配浅色与夜间模式。
- 选章区域新增完整漫画资料，包括 JM 编号、作者、浏览量、点赞数、评论数、发布时间、标签和简介；封面可点击查看大图，作品详情支持展开、折叠和复制。

### 改进

- 搜索结果与选章区域调整为等宽布局，压缩单条搜索结果产生的空白，为漫画资料和评论保留更多空间。
- 评论、作者、标签和导出标题统一使用 OpenCC 转为简体中文，楼中楼回复同样生效。
- 排行榜继续保持每页 20 部作品，分类筛选后按匹配结果分页并保留导航能力。
- 放大侧栏应用标题，完善作品详情折叠状态和分类标签的 WinUI 主题样式。

### 修复

- 修复从排行榜、每周必看或直接链接进入选章页面时，右侧已解析成功但左侧仍显示“还没有搜索结果”的问题。
- 修复作品详情折叠后宽度缩成标题大小，以及长简介只能显示两行、内容无法完整查看的问题。
- 修复部分繁体评论和嵌套回复未转换，以及低对比度分类标签在浅色模式下难以辨认的问题。

### 工程

- 增加 JM 每周推荐、官方类型参数、内容分类解析、排行榜筛选和详情展示测试；自动化测试由 100 个增加到 116 个。

## v2.4.0 — 2026-08-23

### 新功能

- 在线阅读新增条漫模式，支持连续纵向阅读、按视口缩放、阅读位置恢复以及图片失败重试；设置页可将条漫默认缩放设为 30%–200%。
- 搜索结果下方新增漫画评论，支持评论总数、剧透标记、楼中楼回复、刷新以及每页 10 条的上一页/下一页导航。
- 排行榜改为每页 20 部作品，保留全局名次，并在需要时继续从站点加载下一批数据。
- 下载前会检查本地章节：已完整下载的章节自动跳过，部分缺失时仅补下载缺少内容，并在页面顶部明确提示本次处理结果。
- 书库详情新增“删除本地漫画”，会将漫画及同一站点 ID 的重复目录一并移入 Windows 回收站；下载任务仍在运行时会阻止误删。
- 书库中的收藏漫画增加星标、高亮边框和“已收藏”标签，更容易与普通漫画区分。

### 修复

- 重写下载暂停、继续与停止的状态衔接：暂停后不再继续发起图片请求，继续后能恢复下载，反复操作或从停止状态恢复时不再卡在 0 MB/s。
- 修复同一漫画因目录名称变化而在书库中重复出现的问题，统一按站点与漫画 ID 合并显示，并保留重复目录数量。
- 下载历史会同时显示本地累计完成章数与本次补下载章数，未完成记录再次下载后可正确补齐并更新状态。
- 阅读进度可跨重复目录匹配同一漫画，目录合并后仍能找回上次阅读位置。
- 修复若干 WinUI 交互细节，包括按钮状态不同步、转换器反向绑定异常及侧栏标题旁的空白图标。

### 界面与工程

- 调整搜索结果、评论与选章区域比例，为评论内容保留更多空间，并完善书库删除确认、空状态和危险操作样式。
- 固定 .NET SDK 版本，测试宿主改为自带 Windows App SDK Runtime，CI 不再修改 runner 的系统包注册；自动化测试由 80 个增加到 100 个。

## v2.3.1 — 2026-08-22

### 修复

- 打 tag 发布时不再跳过测试：CI 的打包作业改为等测试通过后才启动。此前两个作业并行，测试失败也会照样构建并发布安装包。
- 站点服务的专辑缓存与 API 域名故障转移状态不再分裂：`JmComicService` 之前被注册为 transient 却由两个单例各自持有一份，导致搜索预热的缓存下载器看不到，且已学到的可用域名会丢失、每次都从第一个域名重试。
- 图片乱序规则解析失败时会写入告警，不再静默回退默认值。站点改版后原先的表现是「下载全部成功、日志无错误、图片却全是乱块」。
- 注入的站点配置不再被忽略：响应解密此前始终读取默认密钥。
- 修复排行榜页与下载页的内存泄漏：排行榜每访问一次就会泄漏一个页面及其全部封面图；下载页离开后状态轮询仍在继续，反复切换会叠加多个轮询器。
- 关闭应用时不再在下载/导出仍在运行时释放其取消令牌，避免后台线程报错并留下 `.part` 残件。
- CBZ 导出现在可以真正取消，界面新增「取消」按钮。此前取消只停止了进度轮询，导出线程仍在继续写文件。
- 修复阅读器切章时污染阅读进度：分页模式下上一章的图片回调可能把旧页码写进新章节。
- 下载任务列表恢复按创建时间倒序。此前排序键是随机的任务号，离开下载页再回来顺序就乱了。
- 漫画或章节标题命中 Windows 保留设备名（如 `NUL`、`CON.jpg`）时不再导致下载失败；标题截断不再切开 emoji。声明长路径支持以缓解超长标题的路径长度限制。
- 修复选章面板的文字重影：解析漫画时空状态（或上一部漫画的内容）会与「正在获取…」遮罩同时显示。
- 修复日志级别配置失效，此前无法开启 Debug 级别排查问题。

### 性能

- 书库枚举移出界面线程，大量漫画时不再卡住界面。
- 下载任务面板去掉两处高频开销：日志改为增量追加，不再每秒整表重建列表；章节匹配去掉平方级查找。

### 架构

- 界面线程调度抽象为 `IDispatcher` 并改为构造函数注入，解除了三个主要 ViewModel 无法进入单元测试的限制。
- 自动化测试从 59 个增加到 80 个，新增离线 HTTP 传输层替身，确保测试不会访问真实站点。
- 移除后端内联化后已无法触达的异常分支。

## v2.3.0 — 2026-08-18

### 新功能

- 在线阅读：选章面板新增「在线阅读」，无需下载即可在分页模式直接翻看站点漫画，支持预加载、进度记忆与失败重试。
- 设置页新增「图片下载并发数」与「章节失败重试次数」，下载调度即时生效。
- 书库支持收藏标记：收藏的漫画在列表中置顶，详情面板可一键收藏/取消。
- 书库列表与详情显示「上次读到第 N 话 · 第 M 页」，打开阅读器自动回到上次位置。
- 下载页搜索框支持直接输入 JM 编号或链接，精确解析漫画。
- 排行榜条目显示名次序号与作者，搜索结果列表作者信息更清晰。
- 阅读器：分页模式点击图片右半翻下一页、左半翻上一页；支持 PageUp/PageDown 翻页、Home/End 跳章节首末页；分页模式自动预加载下一张图片。
- 阅读器：分页模式新增缩放（Ctrl + 滚轮或顶部按钮，50%–300%），缩放后可滚动查看细节，在线阅读与本地阅读均支持。
- 排行榜：榜单内容与所选分类在页面切换间保持，切走再切回不丢失。

### 架构

- 将站点访问、下载调度、书库、CBZ 导出和阅读访问拆分为职责独立的 C# 服务，界面调用契约保持兼容。
- 接入 Microsoft.Extensions.Logging 文件日志，未处理异常和关键失败路径会写入本地日志文件，日志按天分文件并保留最近 7 天。
- 移除 `App.GetService<T>()` 服务定位器，全部改为构造函数注入。
- 移除旧后端遗留的健康检查，启动状态直接读取本地设置。
- 禁漫天堂站点协议（域名、密钥、签名与图片乱序规则）抽为 `JmSiteOptions` 集中配置。
- 发布流程：打 tag 时自动注入版本号到程序集与安装包，CI 预装 Windows App Runtime 修复测试挂起。

### 界面

- 设置页显示内置服务和下载目录，不再显示服务地址与进程控制。
- 完善窗口标题栏的夜间模式适配。

## v2.2.0 — 2026-05-24

### 新功能

- 启动画面、页面切换动画和搜索历史管理。
- 加载状态、空状态及响应式布局优化。

### 修复与维护

- 修正安装路径检测和 CI 发布权限。
- 使用 partial property 适配 CommunityToolkit.Mvvm。

## v2.1.0 — 2026-05-03

### 架构重构

- 引入 WinUI 3 与 MVVM 桌面架构。
- 增加任务进度、CBZ 导出、本地书库和设置持久化。
- 支持禁漫天堂。

## v2.0.0 — 2026-04

- 桌面界面和批量下载。
- 网络重试、代理支持、下载恢复与章节命名。

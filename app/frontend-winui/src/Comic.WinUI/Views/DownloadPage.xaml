<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Comic.WinUI.Views.DownloadPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Comic.WinUI.Controls"
    mc:Ignorable="d">

    <Grid Padding="24,16,24,24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Search Bar -->
        <controls:SearchBarControl x:Name="SearchBarControl1" Grid.Row="0" ViewModel="{x:Bind ViewModel, Mode=OneWay}" />

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="0,12,0,0" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="360" />
            </Grid.ColumnDefinitions>

            <!-- Left: Search Results -->
            <controls:SearchResultsList x:Name="SearchResultsList1" Grid.Column="0" ViewModel="{x:Bind ViewModel, Mode=OneWay}" />

            <!-- Right: Detail + Chapters + Tasks -->
            <Grid Grid.Column="1" RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="2*" />
                    <RowDefinition Height="*" MinHeight="280" />
                </Grid.RowDefinitions>

                <!-- Manga Detail + Chapter Selection -->
                <controls:MangaDetailCard Grid.Row="0" ViewModel="{x:Bind ViewModel, Mode=OneWay}" />

                <!-- Task List -->
                <controls:TaskListPanel Grid.Row="1" Margin="0,12,0,0" ViewModel="{x:Bind ViewModel, Mode=OneWay}" />
            </Grid>
        </Grid>
    </Grid>
</Page>

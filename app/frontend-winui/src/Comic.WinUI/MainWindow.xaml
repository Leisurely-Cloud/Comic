<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Comic.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Comic.WinUI.Controls">

    <Grid x:Name="RootGrid"
          Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <!-- Splash Screen -->
        <Grid x:Name="SplashScreen" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="16">
                <!-- 文字图标 -->
                <Border Width="128" Height="128"
                        CornerRadius="24"
                        Background="{ThemeResource AccentFillColorDefaultBrush}">
                    <TextBlock Text="漫"
                               FontSize="72"
                               FontWeight="Bold"
                               Foreground="White"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center" />
                </Border>
                <TextBlock Text="漫画下载器"
                           FontSize="28"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                <TextBlock Text="正在加载..."
                           FontSize="14"
                           Opacity="0.6"
                           HorizontalAlignment="Center" />
                <ProgressRing IsActive="True"
                              Width="32" Height="32"
                              HorizontalAlignment="Center" />
            </StackPanel>
        </Grid>

        <!-- Main Content -->
        <Frame x:Name="MainFrame" Visibility="Collapsed" />
    </Grid>
</Window>
